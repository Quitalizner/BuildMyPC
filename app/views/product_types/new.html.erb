<% provide(:title,"Add Type") %>
<% provide(:content,"Add Type") %>
<% provide(:button,"Create") %>
<% provide(:url,"form_for(@type, url: product_types_path)")%>
<%= render 'form' %>  <!-- locals sends the parameter @type through by defining type --> 
