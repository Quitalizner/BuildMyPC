<div class="row">
  <aside class="col-md-4">
    <section>
      <h1>
		<%= image_tag(@product.image_url(:large)) %>
		<%= @product.name %>
		<%= @product.description %>
		<%= @product.price %>
      </h1>
    </section>
  </aside>
</div>
